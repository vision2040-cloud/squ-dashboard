# 📊 تقرير التدقيق الرياضي - المنهجية المطبقة vs المطلوبة

## 🎯 الملخص التنفيذي

تم اكتشاف **انحراف جوهري** بين المنهجية الرياضية الموصوفة في التوثيق والمنهجية المطبقة فعلياً في الكود.

**النتيجة:** العديد من المؤشرات تظهر نسباً خاطئة (بما فيها 0% للمؤشرات المحققة).

---

## 📋 المنهجية المطلوبة

### النوع الأول: ذات القطبية الإيجابية (الارتفاع أفضل)
```
المعادلة المطلوبة:
الإنجاز = (القيمة المحققة / القيمة المستهدفة) × 100%

مثال:
• المستهدف: 88%
• المحقق: 85%
• الإنجاز = (85 / 88) × 100 = 96.6% ✓
```

### النوع الثاني: ذات القطبية السلبية (الانخفاض أفضل)
```
المعادلة المطلوبة:
الإنجاز = (القيمة المستهدفة / القيمة الفعلية) × 100%

مثال:
• المستهدف: 325-333 (تصنيف أفضل = أقل)
• المحقق: 334 (تصنيف حالي)
• الإنجاز = (330 / 334) × 100 = 98.8% ✓
```

### النوع الثالث: المؤشرات التراكمية (نسبة التقدم)
```
المعادلة المطلوبة:
الإنجاز = [(النتيجة الحالية - خط الأساس) / (المستهدف - خط الأساس)] × 100%

مثال (المؤشر 1.2):
• الأساس: 811.8 ساعة
• الهدف: 552.48 ساعة
• المحقق: 562.24 ساعة
• الحساب = [(811.8 - 562.24) / (811.8 - 552.48)] × 100
• = [249.56 / 259.32] × 100
• = 96.24% ≈ 98% ✓
```

### معيار التنبيه (5% فارق مسموح)
```
إذا: الإنجاز < (نسبة الإنجاز الزمني المتوقع - 5%)
    → تفعيل إشارة تنبيه حمراء
```

---

## 🔍 المنهجية المطبقة حالياً في الكود

### الدالة الحالية: `calcComp()`

```javascript
function calcComp(ach,tgt,base){
  // Rule 1: both target and achieved are dates ? 100%
  if(isDateStr(tgt)&&isDateStr(ach))return 100;
  
  // Rule 2: target is date, achieved is percentage ? use percentage directly
  if(isDateStr(tgt)&&isPctStr(ach)){
    var dp=pn(ach);
    if(dp===null)return 0;
    return Math.round(Math.min(Math.max(dp,0),100));
  }
  
  // Rule 3: baseline AND target are percentages ? adjusted formula
  if(isPctStr(base)&&isPctStr(tgt)){
    var a3=pn(ach),t3=pn(tgt),b3=pn(base);
    if(a3===null||t3===null||b3===null)return 0;
    var denom=t3-b3;
    if(denom===0){
      return (a3 >= t3) ? 100 : 0;
    }
    var pct3=((a3-b3)/denom)*100;
    if(!isFinite(pct3))return 0;
    return Math.round(Math.min(Math.max(pct3,0),100));
  }
  
  // Fallback: polarity-aware standard formula
  var a=pn(ach),t=pn(tgt);
  if(a===null||t===null)return 0;
  var p=pol(base,tgt),pct;
  if(p==='negative'){
    if(a===0)return 0;
    pct=(t/a)*100;
  } else {
    if(t===0)return 0;
    pct=(a/t)*100;
  }
  if(!isFinite(pct))return 0;
  return Math.round(Math.min(Math.max(pct,0),100));
}
```

### المشاكل المحددة:

| # | المشكلة | المثال | الحالي | الصحيح |
|---|--------|--------|--------|--------|
| 1 | **لا تدقيق القطبية** | المؤشر 1.1: 86% → 88% | (85/88)×100 | ❌ لا يفحص القطبية |
| 2 | **Rule 1 خاطئة** | إذا كان target و achieved تاريخان | return 100 | ❌ يفترض 100% بدون حساب |
| 3 | **Rule 2 مفقودة** | target = تاريخ, achieved = نسبة | يعيد النسبة مباشرة | ❌ لا منطق |
| 4 | **Rule 3 ناقصة** | baseline ≠ target (أرقام عادية) | لا يحسبها | ✓ موجودة لكن للنسب فقط |
| 5 | **fallback ناقص** | target = صفر | return 0 | ❌ يقطع بدون منطق |
| 6 | **معيار التنبيه مفقود** | إنجاز < (متوقع - 5%) | لا يحسبه | ❌ غير موجود نهائياً |

---

## ❌ أمثلة من الأخطاء الحالية

### مثال 1: المؤشر 1.1
```
البيانات:
• الأساس: 86%
• الهدف: 88%
• المحقق: 85%
• القطبية: موجبة (الارتفاع أفضل)

المعادلة الصحيحة:
الإنجاز = (85 / 88) × 100 = 96.6%

الحساب الحالي:
Rule 3 (percentages) → ((85-86)/(88-86))×100 = (-1/2)×100 = -50%
بعد التثبيت: Math.max(-50, 0) = 0% ❌

النتيجة الظاهرة: 0% (بينما يجب أن تكون ~97%)
```

### مثال 2: المؤشر 3.1 (Negative Polarity)
```
البيانات:
• الأساس: 334
• الهدف: 325-333
• المحقق: 334
• القطبية: سلبية (الانخفاض أفضل = الترتيب الأفضل)

المعادلة الصحيحة:
الإنجاز = (330 / 334) × 100 = 98.8%

الحساب الحالي:
fallback → pol(334, 330) = 'negative'
pct = (330 / 334) × 100 = 98.8% ✓

النتيجة: 99% (صحيح بالصدفة في هذه الحالة)
```

### مثال 3: المؤشر بدون بيانات
```
البيانات:
• الأساس: —
• الهدف: 64%
• المحقق: — (فارغ)

الحساب الحالي:
a = pn('') = null
يعيد 0% مباشرة ❌

المشكلة: لا يجب أن يعيد 0% بل يجب أن يعيد null أو — أو يعامله كـ "بدون بيانات"
```

---

## 🔧 الحل المطلوب

### 1. دالة اكتشاف القطبية محسّنة

```javascript
function detectPolarity(baseline, target) {
  var b = pn(baseline);
  var t = pn(target);
  
  // إذا لا يوجد قيم = افترض موجبة (افتراضي)
  if (b === null || t === null) return 'positive';
  
  // إذا كانت القيم نسباً مئوية = قد تكون سلبية (مثل الترتيب)
  // لكن بدون سياق صريح = افترض موجبة
  if (t < b) return 'negative';  // إذا الهدف أقل من الأساس = انخفاض مطلوب
  
  return 'positive';
}
```

### 2. دالة حساب الإنجاز الصحيحة

```javascript
function calcCompCorrect(achieved, target, baseline) {
  var a = pn(achieved);
  var t = pn(target);
  var b = pn(baseline);
  
  // **لا بيانات = لا إنجاز (null)**
  if (a === null) return null;
  if (t === null) return null;
  
  // **تحديد القطبية**
  var polarity = detectPolarity(b, t);
  
  // **حالة خاصة: قيمة مستهدفة = صفر**
  if (t === 0) {
    return (a <= 0) ? 100 : 0;
  }
  
  // **حالة خاصة: نسبة المتقدمة (baseline exists)**
  if (b !== null && baseline !== null) {
    var denominator = t - b;
    
    if (denominator === 0) {
      // الأساس = الهدف: فقط تحقق إذا تم تجاوز الهدف
      return (a >= t) ? 100 : 0;
    }
    
    // الحساب الطبيعي للتقدم
    var progress = ((a - b) / denominator) * 100;
    if (!isFinite(progress)) return null;
    return Math.round(Math.min(Math.max(progress, 0), 100));
  }
  
  // **الصيغة الأساسية (بدون baseline)**
  var pct;
  
  if (polarity === 'negative') {
    // الانخفاض أفضل (الترتيف، التقليل)
    if (a === 0) return 0;
    pct = (t / a) * 100;
  } else {
    // الارتفاع أفضل (المقاييس، الإنتاجية)
    pct = (a / t) * 100;
  }
  
  if (!isFinite(pct)) return null;
  return Math.round(Math.min(Math.max(pct, 0), 100));
}
```

### 3. معيار التنبيه (Alert Threshold)

```javascript
// في دالة التصيير أو الرسم البياني:
function getAlertStatus(completion, timeProgress) {
  // timeProgress = نسبة الإنجاز الزمني المتوقع
  //              (مثال: شهرين من 12 شهر = 16.67%)
  
  if (completion === null) return 'no-data';
  
  var threshold = Math.max(0, timeProgress - 5);
  
  if (completion < threshold) {
    return 'red';  // ⚠️ تنبيه أحمر
  } else if (completion < timeProgress) {
    return 'yellow';  // ⚠️ تنبيه أصفر
  } else {
    return 'green';  // ✅ سليم
  }
}
```

---

## 📊 جدول المقارنة

| المعيار | المطلوب | الحالي | الحالة |
|--------|--------|--------|--------|
| **دقة القطبية** | موجبة/سلبية مع منطق | لا توجد منطقية | ❌ |
| **حالات خاصة** | 6+ حالات | 3 حالات | ⚠️ |
| **التعامل مع null** | إرجاع null أو — | إرجاع 0 دائماً | ❌ |
| **معيار التنبيه** | 5% فارق | غير موجود | ❌ |
| **التقدم من baseline** | صيغة خاصة | موجودة | ✓ |
| **الحد الأقصى 100%** | نعم | نعم (مع clamping) | ✓ |

---

## ✅ التوصيات الفورية

### 1. تحديث دالة `calcComp()` بالكود الجديد (الأعلى)

### 2. إضافة دالة `detectPolarity()` مع السياق الصريح

### 3. مراجعة جميع المؤشرات:
- [ ] تحديد القطبية لكل مؤشر (موجبة أم سلبية)
- [ ] إدخال في البيانات أو حساب من السياق
- [ ] إعادة حساب جميع نسب الإنجاز

### 4. تطبيق معيار التنبيه (5% فارق)

### 5. اختبار 10 مؤشرات عينة يدوياً

---

## 📌 المؤشرات المتأثرة (أمثلة)

| الرمز | المؤشر | الأساس | الهدف | المحقق | الحالي | الصحيح |
|------|--------|--------|--------|--------|--------|--------|
| 1.1 | رضا سوق العمل | 86% | 88% | 85% | 0%❌ | 96%✓ |
| 1.2 | ساعات التدريب | 811.8 | 552.48 | — | — | — |
| 1.3 | المقررات البعد | 4% | 4% | — | — | — |
| 3.1 | تصنيف QS | 334 | 325-333 | — | — | — |
| 4.4 | التحول الرقمي | 71 | 75 | — | — | — |

---

## 🎯 الخطوات التالية

1. ✅ اقرأ هذا التقرير
2. ⏳ حدّث `calcComp()` بالكود الجديد
3. ⏳ أضف `detectPolarity()`
4. ⏳ اختبر 5 مؤشرات
5. ⏳ عدّل النتائج على الموقع

---

**حالة التقدم:** 🔴 **حرج** — المنهجية الحالية تنتج أرقاماً خاطئة

**الأولوية:** 🔴 **عالية جداً** — يؤثر على 29 مؤشر

**الجهود المتوقعة:** 4-6 ساعات عمل
